let arr = [1, 2, 3, 4, 5];

let arrLength = len(arr);
puts(arrLength);

let firstElement = first(arr);
puts(firstElement);

let lastElement = last(arr);
puts(lastElement);

let restArr = rest(arr);
puts(restArr);

let pushArr = push(arr, 6);
puts(pushArr);


let emptyArr = [];
puts(first(emptyArr));
puts(last(emptyArr));
puts(rest(emptyArr));